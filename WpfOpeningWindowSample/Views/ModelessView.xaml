<Window x:Class="WpfOpeningWindowSample.Views.ModelessView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfOpeningWindowSample.ViewModels"
        xmlns:local="clr-namespace:WpfOpeningWindowSample.Views"
        mc:Ignorable="d"
        Title="ModelessView" Height="300" Width="300">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Button x:Name="btnClose" Content="Close" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center" Width="237" Command="{Binding CloseCommand}" Height="202" FontSize="48"/>

    </Grid>
</Window>
